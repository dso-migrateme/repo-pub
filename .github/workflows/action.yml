name: Local

on:
  workflow_dispatch: {}
  pull_request: {}
  pull_request_target: {}
  push:
    branches:
      - '**'

jobs:
  test:
    runs-on: ubuntu-24.04
    steps:
      - name: local-step
        run: |
          echo "Local step"

